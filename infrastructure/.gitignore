# Terraform state and local data
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup

# Sensitive values — copy terraform.tfvars.example to terraform.tfvars and fill in your values
terraform.tfvars

# Lambda zip — regenerate with: Compress-Archive -LiteralPath .\lambda_function.py -DestinationPath .\lambda.zip
lambda/lambda.zip

# OS junk
.DS_Store
Thumbs.db
